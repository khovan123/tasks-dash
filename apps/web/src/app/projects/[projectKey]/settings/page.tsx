import { Card, CardContent, CardHeader } from "@/components/atoms/card";
import { Button } from "@/components/atoms/button";
import { ProjectShell } from "@/components/organisms/project-shell";
import { projectData } from "@/features/demo/demo-data";
export default async function SettingsPage({params}:{params:Promise<{projectKey:string}>}){const{projectKey}=await params;const{project}=projectData(projectKey);return <ProjectShell project={project} active="settings"><Card><CardHeader><div><h2 className="font-bold">Project details</h2><p className="text-sm text-slate-500">Key, repository and Drive configuration</p></div></CardHeader><CardContent className="max-w-2xl space-y-4"><label className="block text-sm font-semibold">Project key<input readOnly value={project.key} className="mt-2 h-10 w-full rounded-md border bg-slate-50 px-3"/></label><label className="block text-sm font-semibold">GitHub repository<input defaultValue={project.repositoryFullName} className="mt-2 h-10 w-full rounded-md border px-3"/></label><label className="block text-sm font-semibold">Google Drive root folder<input defaultValue={project.driveRootFolderId} className="mt-2 h-10 w-full rounded-md border px-3"/></label><Button>Save changes</Button></CardContent></Card></ProjectShell>}
