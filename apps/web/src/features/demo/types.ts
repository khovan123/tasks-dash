import { AutomationAction, AutomationExecutionMode, AutomationTrigger, GithubPullRequestState, MemberRole, Priority, SprintState, WorkflowCategory, WorkItemType } from "@tasks-dash/contracts";
export interface DemoMember { id: string; name: string; email: string; avatarUrl: string; role: MemberRole; status: string; projectKeys: string[] }
export interface DemoProject { key: string; name: string; description: string; color: string; health: string; repositoryFullName: string; driveRootFolderId: string; progress: number; completedItems: number; totalItems: number; openPrItems: number; activeSprint: string; memberIds: string[] }
export interface DemoStatus { id: string; name: string; category: WorkflowCategory; color: string; order: number }
export interface DemoWorkItem { id: string; key: string; projectKey: string; type: WorkItemType; summary: string; description: string; statusId: string; priority: Priority; assigneeId: string | null; reporterId: string; moduleId: string | null; parentId: string | null; sprintId: string | null; labels: string[]; storyPoints: number | null; dueDate: string | null; githubPullRequest: { number: number; url: string; state: GithubPullRequestState } | null }
export interface DemoSprint { id: string; projectKey: string; name: string; goal: string; state: SprintState; startDate: string; endDate: string; capacity: number }
export interface DemoAutomation { id: string; projectKey: string; name: string; enabled: boolean; trigger: AutomationTrigger; executionMode: AutomationExecutionMode; runCount: number; actions: { type: AutomationAction; config: Record<string, unknown> }[] }
export interface DemoData { projects: DemoProject[]; members: DemoMember[]; statuses: DemoStatus[]; workItems: DemoWorkItem[]; sprints: DemoSprint[]; automations: DemoAutomation[]; dailyActivity: { day: string; TD: number; SK: number; FW: number }[] }
