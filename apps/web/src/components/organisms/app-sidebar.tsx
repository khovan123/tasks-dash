"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Bell, Boxes, ChevronDown, FolderKanban, LayoutDashboard, Plus, Search, Settings } from "lucide-react";
import { cn } from "@/lib/utils";
import type { DemoProject } from "@/features/demo/types";
export function AppSidebar({ projects }: { projects: DemoProject[] }) { const pathname = usePathname(); return <aside className="fixed inset-y-0 left-0 z-30 flex w-[260px] flex-col border-r bg-[#111827] text-slate-200"><div className="flex h-16 items-center gap-3 border-b border-white/10 px-5"><div className="grid size-9 place-items-center rounded-lg bg-indigo-500"><Boxes size={20}/></div><div><p className="font-bold text-white">Tasks Dash</p><p className="text-[11px] text-slate-400">Workspace</p></div><ChevronDown className="ml-auto" size={16}/></div><div className="p-3"><button className="flex h-9 w-full items-center gap-2 rounded-md bg-white/5 px-3 text-sm text-slate-300"><Search size={15}/>Search work items</button></div><nav className="space-y-1 px-3"><Link href="/" className={cn("flex items-center gap-3 rounded-md px-3 py-2 text-sm", pathname === "/" ? "bg-indigo-500 text-white" : "hover:bg-white/5")}><LayoutDashboard size={17}/>Portfolio overview</Link><Link href="/settings/integrations" className={cn("flex items-center gap-3 rounded-md px-3 py-2 text-sm", pathname.startsWith("/settings") ? "bg-indigo-500 text-white" : "hover:bg-white/5")}><Settings size={17}/>Integrations</Link></nav><div className="mt-5 flex items-center justify-between px-6 text-[11px] font-bold uppercase tracking-widest text-slate-500"><span>Projects</span><Plus size={14}/></div><div className="mt-2 flex-1 space-y-1 overflow-y-auto px-3">{projects.map((project) => { const active = pathname.includes(`/projects/${project.key}`); return <Link key={project.key} href={`/projects/${project.key}`} className={cn("flex items-center gap-3 rounded-md px-3 py-2 text-sm", active ? "bg-white/10 text-white" : "hover:bg-white/5")}><span className="grid size-7 place-items-center rounded-md text-xs font-black text-white" style={{ backgroundColor: project.color }}>{project.key.slice(0,2)}</span><span className="min-w-0 flex-1 truncate">{project.name}</span><FolderKanban size={14} className="text-slate-500"/></Link>})}</div><div className="border-t border-white/10 p-4"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-full bg-indigo-100 font-bold text-indigo-700">MP</div><div className="min-w-0"><p className="truncate text-sm font-semibold text-white">Minh Phan</p><p className="truncate text-xs text-slate-500">Workspace owner</p></div><Bell className="ml-auto" size={16}/></div></div></aside>; }
