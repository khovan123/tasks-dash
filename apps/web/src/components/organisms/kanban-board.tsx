import type { DemoMember, DemoStatus, DemoWorkItem } from "@/features/demo/types";
import { WorkItemCard } from "@/components/molecules/work-item-card";
export function KanbanBoard({ statuses, items, members }: { statuses: DemoStatus[]; items: DemoWorkItem[]; members: DemoMember[] }) { return <div className="grid min-w-[980px] grid-cols-4 gap-4">{statuses.filter((status) => status.id !== "backlog").map((status) => { const columnItems = items.filter((item) => item.statusId === status.id); return <section key={status.id} className="rounded-xl bg-slate-100/80 p-3"><div className="mb-3 flex items-center justify-between"><div className="flex items-center gap-2"><span className="size-2 rounded-full" style={{ backgroundColor: status.color }}/><h3 className="text-sm font-bold">{status.name}</h3></div><span className="text-xs font-semibold text-slate-500">{columnItems.length}</span></div><div className="space-y-3">{columnItems.map((item) => <WorkItemCard key={item.id} item={item} member={members.find((member) => member.id === item.assigneeId)}/>)}{columnItems.length === 0 && <div className="rounded-lg border border-dashed bg-white/50 p-6 text-center text-xs text-slate-400">Drop work item here</div>}</div></section>})}</div>; }
